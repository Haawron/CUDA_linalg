#include <iostream>
#include <random>
using namespace std;

int main() {
    cout << rand() / (double)RAND_MAX << endl;
    cout << rand() / (double)RAND_MAX << endl;
    cout << rand() / (double)RAND_MAX << endl;
    cout << rand() / (double)RAND_MAX << endl;
}